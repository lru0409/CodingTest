#include <iostream>
using namespace std;

int main (void)
{
	int n;
	cin >> n;

	if (n == 0)
		cout << "YONSEI" << endl;
	else if (n == 1)
		cout << "Leading the Way to the Future" << endl;

	return (0);
}