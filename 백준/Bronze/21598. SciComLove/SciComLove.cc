#include <iostream>
using namespace std;

int main (void)
{
	ios_base::sync_with_stdio(false);
	
	long long n;
	cin >> n;

	for(int i = 0; i < n; i++)
		cout << "SciComLove" << endl;
	
	return (0);
}